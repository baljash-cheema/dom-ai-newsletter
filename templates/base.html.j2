<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>DOM Education Newsletter — {{ pages[0].meta.get('month_year', '') if pages else '' }}</title>
<style>
{{ css }}
</style>
</head>
<body class="{{ 'pdf' if for_pdf else 'screen' }}{{ ' draft' if is_draft else '' }}">

{% if is_draft %}<div class="draft-banner">DRAFT — {{ verified }}/{{ total }} claims verified · not for distribution</div>{% endif %}

{% for page in pages %}{% include "page.html.j2" %}{% endfor %}

</body>
</html>
